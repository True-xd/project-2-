# Core
streamlit==1.30.0
pandas==2.1.1

# Optional / Enhancements
numpy==1.26.0          # for numerical operations if needed
matplotlib==3.8.0      # for charts/visualization (optional)
seaborn==0.12.2        # for advanced visualization (optional)
openpyxl==3.1.3        # if you export data to Excel (optional)

# Database (SQLite is built-in, no separate install needed)
